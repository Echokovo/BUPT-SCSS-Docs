## Exercise 1

6 tokens

## Exercise 2

1. n+1
2. n-1
3. 1
4. 1
5. $\begin{cases} 1 & \operatorname{if}m<n \\ 0 & \operatorname{if}m=n & \end{cases}$
6. $\frac{n(n+1)}{2}$
7. $2^n$

## Exercise 3

1. 全集为 $\sum =\{a,b\}$，以字符 $b$ 为结尾的所有字符串
1. 全集为 $\sum =\{a,b\}$，以字符串 aa、ab、ba、bb 为结尾的长度大于等于 2 的所有字符串
1. 全集为 $\sum =\{a,b\}$，包含三个字符 b 的长度大于等于 3 的所有字符串

## Exercise 4

1. $86-010-[1-9][0-9]^7$
2. $a(a|b)^*b$
3. $[a-z]^*a[a-z]^*e[a-z]^*i[a-z]^*o[a-z]^*u[a-z]^*$

## Optional Exercise 1

$$
(\epsilon|a)(\epsilon|b)(\epsilon|c)|
(\epsilon|a)(\epsilon|c)(\epsilon|b)|
(\epsilon|b)(\epsilon|a)(\epsilon|c)|
(\epsilon|b)(\epsilon|c)(\epsilon|a)|
(\epsilon|c)(\epsilon|a)(\epsilon|b)|
(\epsilon|c)(\epsilon|b)(\epsilon|a)|
$$
